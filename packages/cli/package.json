{
  "name": "cursor-translate",
  "version": "0.2.5",
  "description": "CLI for cursor-translate token-saving layer",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/davlet42/cursor-translate.git",
    "directory": "packages/cli"
  },
  "bin": {
    "cursor-translate": "./dist/cli.js"
  },
  "files": [
    "dist",
    "plugin",
    "templates"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepack": "npm run build && bash ../../scripts/prepare-publish.sh",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@cursor-translate/core": "0.2.5"
  },
  "optionalDependencies": {
    "@cursor-translate/mcp": "0.2.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
